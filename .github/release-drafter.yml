name-template: 'v$RESOLVED_VERSION'

categories:
  # Not in the other repos' configs, and added here because this ships a public API rather than an
  # application. Those configs list `breaking` and `major` under version-resolver only, so a
  # breaking pull request bumps the version and then appears in the notes with no heading saying
  # what it broke. First, because it is the one section a consumer must read.
  - title: '⚠️ Breaking Changes'
    labels:
      - 'breaking'
      - 'major'
  - title: '🚀 Features'
    labels:
      - 'feature'
  - title: '✨ Enhancements'
    labels:
      - 'enhancement'
  - title: '🐛 Bug Fixes'
    labels:
      - 'bug'
  - title: '🛠️ Refactors'
    labels:
      - 'refactor'
  - title: '📚 Documentation'
    labels:
      - 'documentation'
  - title: '🔧 CI / Build'
    labels:
      - 'CI/CD'

change-template: '- $TITLE @$AUTHOR (#$NUMBER)'

no-changes-template: 'No changes in this release.'

version-resolver:
  major:
    labels:
      - 'major'
      - 'breaking'
  minor:
    labels:
      - 'feature'
  patch:
    labels:
      - 'bug'
      - 'refactor'
      - 'enhancement'
  default: patch

template: |
  ## What's Changed

  $CHANGES

  **Full Changelog**: https://github.com/$OWNER/$REPOSITORY/compare/$PREVIOUS_TAG...v$RESOLVED_VERSION
