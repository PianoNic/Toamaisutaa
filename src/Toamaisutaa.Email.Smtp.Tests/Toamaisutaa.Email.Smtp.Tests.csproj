<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="TUnit" />
    <!-- The shipping package needs only the abstractions. Asserting what AddToamaisutaaSmtpEmail
         and the two opt-in calls actually register needs a container to build. -->
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Toamaisutaa.Email.Smtp\Toamaisutaa.Email.Smtp.csproj" />
  </ItemGroup>

</Project>
