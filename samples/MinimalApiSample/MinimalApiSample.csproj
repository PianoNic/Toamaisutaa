<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <!-- MapOpenApi is called directly below, so this is a direct dependency even though
         Toamaisutaa.OpenApi already brings it. The GHSA-v5pm-xwqc-g5wc pin on Microsoft.OpenApi
         travels with that package now and no longer belongs here. -->
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" />
    <!-- Renders the generated document. Sample only - no shipped package takes a dependency on
         anything that reads OpenAPI. -->
    <PackageReference Include="Scalar.AspNetCore" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Toamaisutaa.AspNetCore\Toamaisutaa.AspNetCore.csproj" />
    <ProjectReference Include="..\..\src\Toamaisutaa.OpenApi\Toamaisutaa.OpenApi.csproj" />
    <ProjectReference Include="..\..\src\Toamaisutaa.OpenIdConnect\Toamaisutaa.OpenIdConnect.csproj" />
    <ProjectReference Include="..\..\src\Toamaisutaa.EntityFrameworkCore\Toamaisutaa.EntityFrameworkCore.csproj" />
    <ProjectReference Include="..\..\src\Toamaisutaa.EntityFrameworkCore.Migrations.Sqlite\Toamaisutaa.EntityFrameworkCore.Migrations.Sqlite.csproj" />
  </ItemGroup>

</Project>
